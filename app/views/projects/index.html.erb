<h1>Listing Projects</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= link_to 'Show', entrepreneur_project_url(@entrepreneur, project) %></td>
    <td><%= link_to 'Edit', edit_entrepreneur_project_url(@entrepreneur, project) %></td>
    <td><%= link_to 'Destroy', entrepreneur_project_url(@entrepreneur, project),
				method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Project', new_entrepreneur_project_url(@entrepreneur) %>
